package ai.askdiverge.domain.model.message.incoming.part.table

/** The horizontal alignment of the content of a table column. */
internal enum class TableColumnAlignment {
    LEFT,
    CENTER,
    RIGHT
}
